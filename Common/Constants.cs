namespace Common
{
    public static class Constants
    {
        // Default connection string for PostgreSql docker image
        public const string CONSTR =
            "User ID=postgres;Password=postgres;Host=localhost;Port=5432;Database=duong_test;Pooling=true;";
    }
}
